// project import
import dashboard from './dashboard';
import docentes from './docentes';
import students from './students';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, docentes, students]
};

export default menuItems;
